diff --git a/lib/file.js b/lib/file.js
index 09c43b55c4b355e5c9a695b11feb7e39710ac9b1..5d64fbda381e1d5f02cec3cf75c028545812e3b1 100644
--- a/lib/file.js
+++ b/lib/file.js
@@ -48,8 +48,8 @@ export default class File extends EventEmitter {
 
     const getPieceDownloaded = (pieceIndex) => {
       const len = pieceIndex === pieces.length - 1 ? lastPieceLength : pieceLength
-      if (bitfield.get(pieceIndex)) {
-        // verified data
+      if (bitfield.get(pieceIndex) || !pieces[pieceIndex]) {
+        // verified data (or piece already nulled out after completion)
         return len
       } else {
         // "in progress" data
diff --git a/lib/torrent.js b/lib/torrent.js
index 771a87fa5004f5ff82e9b5c3d9d8b0d70e110dbf..9ca56471a3cef451ffde7e9a6116871718e65d41 100644
--- a/lib/torrent.js
+++ b/lib/torrent.js
@@ -220,10 +220,10 @@ export default class Torrent extends EventEmitter {
     if (!this.bitfield) return 0
     let downloaded = 0
     for (let index = 0, len = this.pieces.length; index < len; ++index) {
-      if (this.bitfield.get(index)) { // verified data
+      const piece = this.pieces[index]
+      if (this.bitfield.get(index) || !piece) { // verified data (or piece nulled after completion)
         downloaded += (index === len - 1) ? this.lastPieceLength : this.pieceLength
       } else { // "in progress" data
-        const piece = this.pieces[index]
         downloaded += (piece.length - piece.missing)
       }
     }
@@ -1702,7 +1702,10 @@ export default class Torrent extends EventEmitter {
       return index => {
         if (!tries || self.bitfield.get(index)) return true
 
-        let missing = self.pieces[index].missing
+        const piece = self.pieces[index]
+        if (!piece) return true
+
+        let missing = piece.missing
 
         for (; ptr < self.wires.length; ptr++) {
           const otherWire = self.wires[ptr]
@@ -1910,7 +1913,7 @@ export default class Torrent extends EventEmitter {
       const req = minWire.requests[i]
       if (req.piece !== index) continue
 
-      this.pieces[index].cancel((req.offset / Piece.BLOCK_LENGTH) | 0)
+      if (this.pieces[index]) this.pieces[index].cancel((req.offset / Piece.BLOCK_LENGTH) | 0)
     }
 
     this.emit('hotswap', minWire, wire, index)
@@ -1938,6 +1941,8 @@ export default class Torrent extends EventEmitter {
     // var endGame = (wire.requests.length === 0 && self.store.numMissing < 30)
 
     const piece = self.pieces[index]
+    if (!piece) return false
+
     let reservation = isWebSeed ? piece.reserveRemaining() : piece.reserve()
 
     if (reservation === -1 && hotswap && self._hotswap(wire, index)) {
